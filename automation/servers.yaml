- alias: Fan mode set on
  trigger:
    - platform: state
      entity_id: input_select.fan_mode
      to: 'on'
  action:
    - service: script.turn_on
      entity_id: script.servers_on

- alias: Fan mode set off
  trigger:
    - platform: state
      entity_id: input_select.fan_mode
      to: 'off'
  action:
    - service: script.turn_on
      entity_id: script.servers_off
